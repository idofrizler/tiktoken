import SwiftUI

extension Color {
    static let creditOrange = Color(red: 218 / 255, green: 119 / 255, blue: 86 / 255)
    static let depletedAlien = Color.white.opacity(0.16)
    static let clockTick = Color.white.opacity(0.44)
}
