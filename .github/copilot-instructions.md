Follow the repository's `AGENTS.md` for architecture, interaction contracts,
validation commands, signing constraints, and security rules.

Keep changes focused on the standalone watchOS target. Never add Claude
credentials to the app, and keep `project.yml` synchronized with the committed
Xcode project whenever target settings or file membership change.
